/**
 * Correlation core subdomain - Core tracking functionality
 *
 * Barrel export for core subdomain containing:
 * - Correlation context management
 * - Correlation ID generation and tracking
 * - Core correlation types and schemas
 */

// Export all runtime implementations
export * from "./core.classes.js";

// Export all types
export type * from "./core.types.js";

// Export all schemas
export * from "./core.schemas.js";
