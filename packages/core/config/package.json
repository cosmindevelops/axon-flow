{
  "name": "@axon/config",
  "version": "0.1.0",
  "description": "Configuration management with type safety and validation",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "browser": {
    "./dist/index.cjs": "./dist/index.js"
  },
  "dependencies": {
    "@axon/errors": "workspace:*",
    "@axon/types": "workspace:*",
    "dotenv": "^17.2.1",
    "zod": "^4.0.17"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && tsup",
    "dev": "tsup --watch",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "type-check": "tsc --noEmit",
    "format": "prettier --write src/",
    "clean": "rm -rf dist"
  }
}
