# Tests Directory

This directory contains integration and end-to-end tests for the monorepo:

- `integration/` - Cross-package integration tests
- `e2e/` - End-to-end system tests
- `fixtures/` - Test data and fixtures
- `utils/` - Testing utilities and helpers

These tests validate the interaction between different packages and the overall system behavior.
