# Configs Directory

This directory contains shared configuration files for the monorepo:

- `eslint/` - ESLint configurations for different package types
- `prettier/` - Prettier configuration
- `typescript/` - TypeScript configurations
- `vitest/` - Testing configurations
- `docker/` - Docker configurations for services

These configurations are shared across all packages and apps to ensure consistency.
