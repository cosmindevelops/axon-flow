# Apps Directory

This directory contains the microservices and applications for Axon Flow:

- `hub/` - Central orchestration service (Puppeteer model)
- `web/` - User-facing web application
- `admin-dashboard/` - Administrative interface
- `agents/` - Individual agent applications

Each app should be independently deployable and follow the Hub-centric architecture pattern.
